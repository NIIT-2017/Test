#include "stdio.h"
include checkAge

main1()
{
	printf("введите ваш возраст: ");
	scanf("%d", &age);
		checkAge ();
		if (return==1)
			printf("Access granted")
		else
			printf("Access denied")
	return0;
}