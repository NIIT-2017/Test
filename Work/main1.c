#Include <stdio.h>
#Include "task1.h"

int main()
{
	int Age;
		Printf("Enter your age/h");
		scanf(%d, &Age);
		if (CheckAge(Age) == 0)
			Printf("Acces granted/n");
		else
			printf("Acces denied/n");
		return 0;
}